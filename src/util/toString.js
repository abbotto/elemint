/**
 * @private
 * @description
 * Check if the subject is of a certain type
 *
 * @param {Array|Object}
 * @return {String}
 */

const toString = Function.prototype.call.bind(Object.prototype.toString);
